#include<stdio.h>
int main(){
    int num,a=1;
    printf("enter the number \n");
    scanf("%d",&num);
    if (num%a==0 || num%num==0)

    {
        printf("the number is a prime number ");    }
        else {
            printf("the number is not a prime number ");
        }
    
return 0;}